# Lightweight dev image — uses volume mounts for hot-reload
FROM node:22-alpine
WORKDIR /app

RUN corepack enable && corepack prepare pnpm@latest --activate

COPY package.json pnpm-lock.yaml pnpm-workspace.yaml ./
RUN pnpm install

EXPOSE 3000
CMD ["pnpm", "dev"]
